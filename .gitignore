# datasets -- these are not tracked with git (their MD5 checksums are, though!)
freeplay.bag
visual_tracking.bag
freeplay.poses.json
visual_tracking.poses.json

*.orig.*

# videos are not tracked by git
videos/
*.mp4
*.mkv
*.kdenlive

# specific dataset post-processing for particular applications
visual_tracking_dataset.json
visual_tracking_full_dataset.json

# sessions purposefully excluded (typically, faulty recordings)
exclude_*
